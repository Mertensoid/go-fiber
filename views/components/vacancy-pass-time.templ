package components

import "time"

templ VacancyPassTime(time time.Time) {
    @VacancyPassTimeStyle()
    <div class="vacancy-pass-time">
        {time.String()}
    </div>
}

templ VacancyPassTimeStyle() {
    <style>
        .vacancy-pass-time {
            height: 28px;
            padding: 5px;
            width: min-content;
            background-color: #30968910;
            color: #309689;
            display: flex;
            align-items: center;
            border-radius: 5px;
            font-size: 16px;
            white-space: nowrap;
        }
    </style>
}